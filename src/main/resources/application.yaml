
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_db
    username: root
    password: kh1970ho1983
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: MySql
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
